import 'package:flutter/material.dart';

class ItemTrackerScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Item Tracker Screen', style: TextStyle(fontSize: 24)),
    );
  }
}
